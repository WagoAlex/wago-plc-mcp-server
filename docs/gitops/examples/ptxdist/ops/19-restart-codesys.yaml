# 19. Restart the CODESYS runtime
# Firmware generation: PTXdist (FW 04.x, build 31). Do not use on a Yocto PLC.
# Risk: DANGEROUS.
# The PLC program stops and starts again. The machine stops during the restart.
#
# Claude creates this file as ops/<id>.yaml. CI runs the method once after the
# merge and then deletes the file.
# This method is dangerous: apply.py refuses it while approved_by is empty.
# With the CI workflow, the merge sets approved_by from the approving reviewer.
# Claude must never write a value in approved_by.
#   Dry run: python scripts/apply.py docs/gitops/examples/ptxdist/ops/19-restart-codesys.yaml
#   Run:     python scripts/apply.py docs/gitops/examples/ptxdist/ops/19-restart-codesys.yaml --execute
id: ex19a0b1
proposed_at: 2026-09-18T10:00:00+00:00
proposed_by: agent-claude
plc_ip: 192.168.42.118
action: invoke_method
method_id: 0-0-codesys3-restart
arguments: {}
requires_human: CRITICAL
approved_by: ''
